import styled from "styled-components";

export const Wrap = styled.path`
width: ${({ width }) => width};
height: ${({height}) => height};
fill: ${({fill}) => fill};
border-radius: 10px 10px 0px 0px;
`